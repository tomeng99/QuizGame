name: Build and Push

on:
  push:
    branches:
      - main
      - dev

# This workflow builds and publishes images only. Deployment is GitOps: ArgoCD
# syncs tomeng99/operations, so going live means bumping the image tag in
# apps/quizgame/<env>/deployments.yaml there.
# ALLOWED_ORIGINS and the HTTP ports are set by the Kubernetes manifests, not
# here - they were only ever needed by the old podman-compose deploy.
env:
  SERVER_IMAGE: ghcr.io/${{ github.repository_owner }}/quizgame-server
  CLIENT_IMAGE: ghcr.io/${{ github.repository_owner }}/quizgame-client
  EXPO_PUBLIC_API_URL: ${{ github.ref_name == 'main' && secrets.PROD_EXPO_PUBLIC_API_URL || secrets.DEV_EXPO_PUBLIC_API_URL }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image tags
        id: image-tag
        run: |
          echo "deploy=${GITHUB_SHA}" >> "$GITHUB_OUTPUT"
          echo "rolling=${GITHUB_REF_NAME}-latest" >> "$GITHUB_OUTPUT"

      - name: Build and push server image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./apps/server/Dockerfile
          push: true
          tags: |
            ${{ env.SERVER_IMAGE }}:${{ steps.image-tag.outputs.rolling }}
            ${{ env.SERVER_IMAGE }}:${{ steps.image-tag.outputs.deploy }}

      - name: Build and push client image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./apps/client/Dockerfile
          push: true
          tags: |
            ${{ env.CLIENT_IMAGE }}:${{ steps.image-tag.outputs.rolling }}
            ${{ env.CLIENT_IMAGE }}:${{ steps.image-tag.outputs.deploy }}
          build-args: |
            EXPO_PUBLIC_API_URL=${{ env.EXPO_PUBLIC_API_URL }}
            UMAMI_WEBSITE_ID=${{ github.ref_name == 'main' && '6db3c650-6547-452d-89ca-498d9e7d7c2d' || '' }}
